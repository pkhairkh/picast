[package]
name = "picast-playback"
version.workspace = true
edition.workspace = true
description = "GStreamer playback engine with V4L2 H.264 hardware decode for PiCast"

[features]
default = []
hw = ["gstreamer", "picast-display/hw"]

[dependencies]
picast-display = { path = "../display", default-features = false }
gstreamer = { version = "0.23", optional = true }
# gstreamer-app = { version = "0.23", optional = true }    # Future: for AppSink/AppSrc
# gstreamer-video = { version = "0.23", optional = true }  # Future: for video caps helpers
# gstreamer-audio = { version = "0.23", optional = true }  # Future: for audio caps helpers
tokio = { workspace = true, features = ["sync", "time"] }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
anyhow = { workspace = true }
url = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "time", "sync"] }
serde_json = { workspace = true }
